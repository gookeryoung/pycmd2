---
description:
alwaysApply: true
enabled: true
updatedAt: 2025-11-25T14:01:40.683Z
provider:
---

# 项目本地化规则

## 概述

pycmd2 是一个 Python 命令行工具集，支持中英文双语环境。本项目已经完成了全面的本地化工作，将所有用户可见的英文文本翻译为中文。

## 代码规范

### 注释和文档字符串

- 所有面向用户的注释、文档字符串和界面文本必须使用中文
- 技术性注释可以使用英文，但应优先考虑中文
- 函数参数和返回值说明应使用中文

```python
# 好的示例
def merge_pdf_files(input_files, output_path):
    """合并多个PDF文件为单个PDF。

    Args:
        input_files: 输入PDF文件列表
        output_path: 输出PDF文件路径

    Returns:
        bool: 是否合并成功
    """
    # 实现细节...
```

### 变量和函数命名
- 变量名、函数名和类名必须保持英文
- 遵循 PEP 8 命名规范
- 避免使用拼音或中英文混合命名

```python
# 好的示例
class FileProcessor:
    def __init__(self):
        self.file_list = []

    def process_files(self):
        for file_path in self.file_list:
            # 处理文件...
```

### 用户界面文本

- 所有面向用户的界面文本必须使用中文
- 使用简洁明了的中文表达
- 保持专业术语的准确性

```python
# 好的示例
ui.button("开始处理", on_click=self.start_process)
ui.label("处理完成")
```

### 错误消息
- 错误消息必须使用中文
- 提供清晰的错误描述和解决建议
- 保持语气友好和专业

```python
# 好的示例
if not file_path.exists():
    logger.error(f"文件不存在: {file_path}")
    ui.notify("请检查文件路径是否正确", type="negative")
```

## 文件结构规范

### 目录命名

- 目录名使用英文小写字母和下划线
- 避免使用中文目录名

### 文件命名

- Python 文件使用英文小写字母和下划线
- 资源文件可根据内容使用中文名

### 模块导入

- 使用英文模块名导入
- 模块内部注释使用中文

## Web 应用规范

### 路由和URL

- URL路径使用英文
- 页面标题和内容使用中文

```python
# 好的示例
class PDFMergeApp(BaseApp):
    ROUTER = "/tools/pdf-merge"  # 英文路由

    def setup(self):
        ui.label("PDF合并工具")  # 中文标题
```

### 前端文本

- 所有前端显示文本使用中文
- HTML属性值保持英文

## 配置文件规范

### 键名

- 配置键名使用英文
- 配置说明和注释使用中文

```python
# 好的示例
class AppConfig(TomlConfigMixin):
    SERVER_PORT: int = 8080  # 服务器端口
    TIMEOUT: int = 30  # 超时时间(秒)
```

## 测试规范

### 测试描述

- 测试函数名使用英文
- 测试文档字符串使用中文

```python
# 好的示例
def test_pdf_merge():
    """测试PDF合并功能。"""
    # 测试代码...
```

## 文档规范

### README 和文档

- 项目标题使用中英文双语
- 文档内容使用中文
- 代码示例中的注释使用中文

## 翻译注意事项

### 术语一致性

- 使用统一的技术术语翻译
- 建立术语表，确保一致性

### 文化适应性

- 考虑中文用户的表达习惯
- 避免直译，注重表达自然

### 保持代码可读性

- 翻译后代码仍应保持清晰可读
- 避免过长的中文文本影响代码排版

## 检查清单

在提交代码前，请确保：

- [ ] 所有面向用户的文本已翻译为中文
- [ ] 变量名和函数名保持英文
- [ ] 错误消息使用中文
- [ ] 界面文本使用中文
- [ ] 代码注释使用中文
- [ ] 文档字符串使用中文
- [ ] 术语使用一致

## 工具和辅助

### 可用资源

- [pycmd2 中文翻译指南](mdc:translation-guide.md)
- [术语表](mdc:terminology.md)
- [代码示例](mdc:code-examples.md)

### 自动化工具

- 使用 `pycmd2-check-localization` 检查本地化完整性
- 使用 `pycmd2-terminology-lint` 检查术语一致性

## 异常情况

### 第三方库

- 第三方库的原始英文内容无需翻译
- 但与这些库交互的用户消息应翻译

### API接口

- 内部API参数名保持英文
- API错误消息翻译为中文

这些规则确保了 pycmd2 项目在保持代码国际标准的同时，为中文用户提供友好的本地化体验。
